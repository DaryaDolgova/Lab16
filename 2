# Lab16
using namespace std;

#include <iostream>

int main()
{
	int  N, i, j, c, b, v;

	cout << "N=";  cin >> N;
	int* A = new int[N];
	for (i = 0; i < N; i++)
	{
		cin >> A[i];
	}
	v = 0;
	for (i = 0; i < N - v; i++)
		{
			c = 0;
			for (j = 0; j < N; j++)
			{
				if (A[i] == A[j]) 
			{
			    c++;
			}
			}
			if (c == 2)
			{
				v++;
				for (j = i; j < N - 1; j++)
				{
					b = A[j + 1];
					A[j + 1] = A[j];
					A[j] = b;
				}
				i=i-1;
			}
		}
	for (i = 0; i < N - v; i++)
		{
			cout<< A[i] << endl;
		}

	return 0;

}
